# frontend — 前端管理后台（Vue 3）

技术栈：Vue 3 + TypeScript + Vite + Element Plus + Pinia + Vue Router 4 + ECharts 5 + Three.js。

五大页面模块：场景管理 / 数据采集 / 数据分析 / OTA 管理 / 远程操控（含运营看板）。

> 当前为 L0 骨架占位。前端将在业务服务层完成后初始化（`npm create vite@latest`，`.env.development` / `.env.production` 配置网关地址 `http://localhost:8080`）。
